# The URL the site will be built for
base_url = "https://propersquid.com"

theme = "DeepThought"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

taxonomies = [
    { name = "categories", feed = true, paginate_by = 10 },
    { name = "tags", fees = true, paginate_by = 10 },
]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here
navbar_items = [
    { code = "en", nav_items = [
        { url = "$BASE_URL/", name = "Home" },
        { url = "$BASE_URL/posts", name = "Posts" },
        { url = "$BASE_URL/docs", name = "Docs" },
        { url = "$BASE_URL/tags", name = "Tags" },
        { url = "$BASE_URL/categories", name = "Categories" },
        { url = "$BASE_URL/resume", name = "Resume" },
    ] },
]

[extra.author]
name = "Scott"
avatar = "images/avatar.png"

[extra.social]
email = "scott@propersquid.com"
github = "scott-wilson"
twitter = "propersquid"
linkedin = "scott-wilson-9876a767"
stackoverflow = "3108288"
mastodon = "https://mastodon.social/@propersquid"
keybase = "propersquid"
